# Port the frontend dev server listens on (default: 5174)
VITE_FRONTEND_PORT=5174

# Port the backend runs on — used by Vite proxy (default: 3002)
VITE_BACKEND_PORT=3002

# Google OAuth client ID (must match backend GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=
