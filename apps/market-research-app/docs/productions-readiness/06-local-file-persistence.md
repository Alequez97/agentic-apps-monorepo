DONE

# Local File Persistence

Current state:

- Users, sessions, competitor outputs, and reports are persisted to local JSON files.
- The app already has a file-backed persistence layer.

Production note:

- This is implemented, but it is not sufficient for production-scale persistence requirements.
