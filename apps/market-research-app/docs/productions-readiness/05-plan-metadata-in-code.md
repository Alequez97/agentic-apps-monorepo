DONE

# Plan Metadata In Code

Current state:

- The app defines `free`, `starter`, `pro`, and `agency` plan metadata in code.
- Competitor limits and some feature flags are represented in the backend.

Production note:

- This is only static metadata. It is not a real subscription or entitlement system.
